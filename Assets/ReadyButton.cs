using UnityEngine;
using UnityEngine.UI;

public class ReadyButton : MonoBehaviour
{
    //public Button readyButton;
    //private PlayerReady playerReady;

    //private void Start()
    //{
    //    playerReady = GetComponent<PlayerReady>();

    //    if (readyButton != null)
    //    {
    //        readyButton.onClick.AddListener(OnReadyButtonClicked);
    //    }
    //}

    //private void OnReadyButtonClicked()
    //{
    //    if (playerReady != null && playerReady.isLocalPlayer)
    //    {
    //        bool newReadyState = !playerReady.IsReady; // Inverse l'état actuel
    //        playerReady.CmdSetReadyState(newReadyState); // Envoie l'état au serveur
    //    }
    //}
}
